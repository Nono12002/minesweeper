input.onLogoEvent(TouchButtonEvent.Pressed, function () {
    if (true) {
    	
    }
})
let Flag_1 = game.createSprite(randint(0, 4), randint(0, 4))
basic.forever(function () {
    basic.showLeds(`
        # # # # #
        # # # # #
        # # # # #
        # # # # #
        # # # # #
        `)
})
